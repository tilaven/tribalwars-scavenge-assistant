# tribalwars-scavenge-assistant
Scavenging Assistant that works in both the browser and the app. It automatically splits your troops across the available scavenging levels so they finish at the same time and fills in the form — you just click "Start". Graphical settings panel (troop reserve + enabling/disabling units) and automatic game-language detection built in.
